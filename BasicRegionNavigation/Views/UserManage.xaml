<UserControl x:Class="BasicRegionNavigation.Views.UserManage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                 
            </ResourceDictionary.MergedDictionaries>
            <Style x:Key="WhiteHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="Black"/>
                <!--<Setter Property="FontWeight" Value="Bold"/>-->
                <Setter Property="Padding" Value="25,0,0,0"/>
                <Setter Property="Height" Value="40"/>
            </Style>
            <Style TargetType="DataGridCell" x:Key="CustomDataGridCellStyle">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <!--<Setter Property="Margin" Value="6,0"/>-->
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="Margin" Value="20,0,0,0"/>
                <Style.Triggers>
                    <!-- 鼠标悬停高亮 -->
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground" Value="MediumPurple"/>
                    </Trigger>

                    <!-- 被选中时的字体颜色和背景 -->
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Foreground" Value="Aqua"/>
                        <!-- ⬅️ 设置选中时字体为白色 -->
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid Margin="10" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="18*" />
                <RowDefinition Height="0.8*" />
            </Grid.RowDefinitions>
            <Border Padding="10,5,0,0" Background="#2e3a44" CornerRadius="10,10,0,0">
                <ContentControl Content="用户管理" Style="{StaticResource SectionHeaderStyle}" />
            </Border>

            <!-- DataGrid -->
            <DataGrid x:Name="UserGrid" Grid.Row="1"
                      
                  ItemsSource="{Binding PagedUsers}"
                  AutoGenerateColumns="False"
                  ScrollViewer.CanContentScroll="False"
                  EnableColumnVirtualization="False" 
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="None"
                  RowBackground="#22313F"
                  AlternatingRowBackground="#2C3E50"
                  Foreground="White"
                  Background="#1E2A38"
                  BorderBrush="#1E2A38"
                  ColumnHeaderHeight="40"
                  CellStyle="{StaticResource CustomDataGridCellStyle}"
                  FontSize="14"
                  ColumnHeaderStyle="{StaticResource WhiteHeaderStyle}"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="编号" Binding="{Binding Index}" Width="1.2*" />
                    <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="2*" />
                    <DataGridTextColumn Header="部门" Binding="{Binding Department}" Width="2*" />
                    <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="2*" />
                    <DataGridTextColumn Header="电话" Binding="{Binding Phone}" Width="2*" />
                    <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="5*" />

                    <DataGridTemplateColumn Header="操作" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="编辑" 
                        Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                        CommandParameter="{Binding}" 
                        Margin="5,0" Padding="5,2" BorderThickness="0" />
                                    <Button Content="删除" 
                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                        CommandParameter="{Binding}" 
                        Margin="5,0" Padding="5,2" Background="#f56c6c" BorderThickness="0" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- 分页控制条 -->
            <StackPanel Grid.Row="2"  Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="添加用户"
                        Width="90"
                        Height="30"
                        Background="YellowGreen"
                        BorderThickness="0"
                        Command="{Binding ShowAddUserDialogCommand}"
                        HorizontalAlignment="Left"
                        Margin="0,0,20,0" />
                <TextBlock Text="{Binding AllUsers.Count, StringFormat=Total {0}}" Foreground="White" Margin="0,5,20,0"/>

                <ComboBox Width="80" SelectedValue="{Binding PageSize}" SelectedValuePath="Content">
                        <ComboBoxItem Content="16" />
                    <ComboBoxItem Content="20" />
                    <ComboBoxItem Content="30" />
                    <ComboBoxItem Content="40" />
                    <ComboBoxItem Content="50" />
                </ComboBox>

                <Button Content="&lt;" Width="30" Command="{Binding PrevPageCommand}" />
                <Button Content="&gt;" Width="30" Margin="5,0" Command="{Binding NextPageCommand}" />

                <TextBlock Text="Page" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                <TextBox Width="40" Text="{Binding CurrentPage, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Center"/>
                <TextBlock Text="{Binding TotalPages, StringFormat=/ {0}}" Foreground="White" VerticalAlignment="Center" Margin="5,0"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
