<UserControl x:Class="BasicRegionNavigation.Views.AlarmMonitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Views"
             xmlns:con="clr-namespace:BasicRegionNavigation.Controls"
             xmlns:prism="http://prismlibrary.com/" 
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                 
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <con:AlarmListControl 
            Grid.Row="0"
            Width="1900" 
            Height="600" 
            Margin="10"
            AlarmItemsSource="{Binding CurrentWarningInfo.AlarmList}" />

            <Border  Margin="10" Background="#1E2A38" CornerRadius="6" Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*"/>
                        <RowDefinition Height="1.7*"/>
                        <RowDefinition Height="1.2*"/>
                        <RowDefinition Height="8*"/>
                    </Grid.RowDefinitions>
                    <ContentControl Content="历史报警" Style="{StaticResource SectionHeaderStyle}" />
                    <Border Grid.Row="1" Style="{StaticResource MyCustomBorderStyle}"  >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30*"/>
                                <!-- 模组选择文本 -->
                                <ColumnDefinition Width="120*"/>
                                <!-- 模组下拉框 -->
                                <ColumnDefinition Width="60*"/>
                                <ColumnDefinition Width="100*"/>
                                <!-- 时间范围文本 -->
                                <ColumnDefinition Width="10*"/>
                                <!-- 开始时间 -->
                                <ColumnDefinition Width="100*"/>
                                <!-- 至 -->
                            <ColumnDefinition Width="60*"/>
                            <ColumnDefinition Width="60*"/>
                            <!-- 导出按钮 -->
                                <ColumnDefinition Width="500*"/>
                        </Grid.ColumnDefinitions>
                            <!-- 模组选择 -->
                            <TextBlock Text="设备" Foreground="White" VerticalAlignment="Center" FontSize="14"/>
                        <ComboBox Grid.Column="1" ItemsSource="{Binding DeviceSelectGroup}" SelectedItem="{Binding SelectDevice}"  VerticalAlignment="Center" Height="30" Margin="20,0"/>

                            <!-- 时间范围 -->
                            <TextBlock Grid.Column="2" Text="时间范围" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>
                            <hc:DateTimePicker Grid.Column="3" Height="30" DateTimeFormat="yyyy-MM-dd HH:mm:ss" hc:InfoElement.Placeholder="开始时间" hc:InfoElement.ShowClearButton="True"
                        Margin="10,0" SelectedDateTime="{Binding Start}" />
                            <TextBlock Grid.Column="4" Text="至" VerticalAlignment="Center" Foreground="White" HorizontalAlignment="Center" FontSize="14"/>
                            <hc:DateTimePicker Grid.Column="5" Height="30" DateTimeFormat="yyyy-MM-dd HH:mm:ss" 
                        Margin="10,0" SelectedDateTime="{Binding End}" />

                            <!-- 查询按钮 -->
                            <Button Grid.Column="6" Content="查询" Width="70" Height="30"
            Background="#2288FF" Foreground="White" BorderThickness="0" Command="{Binding QueryCommand}"/>

                            <!-- 导出按钮 -->
                        <Button Grid.Column="7"
                                Content="导出数据"
                                Width="70"
                                Height="30"
                                Command="{Binding ExportCommand}"
                                Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                        </Grid>
                    </Border>




                    <!-- 表头 -->
                    <Grid Grid.Row="2" Background="#182128">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="1.5*" />
                            <ColumnDefinition Width="2.5*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="序号" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="15"/>
                        <TextBlock Grid.Column="1" Text="时间" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="2" Text="设备" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="3" Text="报警类别" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="4" Text="报警描述" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="5" Text="状态" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                    </Grid>
                <ScrollViewer HorizontalScrollBarVisibility="Auto" Grid.Row="3">
                    <!-- 数据行 -->
                    <ItemsControl  Foreground="White" ItemsSource="{Binding HistoryRowItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,5,2,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="2.5*" />
                                        <ColumnDefinition Width="1*" />
                                    </Grid.ColumnDefinitions>


                                    <TextBlock Text="{Binding ID}"  Foreground="White" VerticalAlignment="Center" Margin="5"/>
                                    <TextBlock Grid.Column="1" Text="{Binding AlarmTime}"  Margin="5,5,60,5"/>
                                    <TextBlock Grid.Column="2" Text="{Binding DeviceName}"  Margin="5,5,20,5"/>
                                    <TextBlock Grid.Column="3" Text="{Binding AlarmType}"  Margin="5,5,20,5"/>
                                    <TextBlock Grid.Column="4" Text="{Binding AlarmDescription}"  Margin="5,5,20,5"/>
                                    <TextBlock Grid.Column="5" Text="{Binding Status}"  Margin="5,5,20,5"/>

                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
            </Border>

    
    </Grid>
</UserControl>
