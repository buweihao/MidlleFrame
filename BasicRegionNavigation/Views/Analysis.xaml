<UserControl x:Class="BasicRegionNavigation.Views.Analysis"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Views"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel LastChildFill="True">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="5" >
                <hc:ClockRadioButton x:Name="MenuDefault" Command="{Binding NavigateCommand}" CommandParameter="Productivity" Content="  产能 &amp;上挂率" Style="{StaticResource NavToggleBButtonStyle}"   
                  GroupName="BMenu" IsChecked="True"/>
                <hc:ClockRadioButton Command="{Binding NavigateCommand}" CommandParameter="ExceptionData" Content="  上挂异常数据  " Style="{StaticResource NavToggleBButtonStyle}"   
                  GroupName="BMenu"/>
                <hc:ClockRadioButton Command="{Binding NavigateCommand}" CommandParameter="HangerQuery" Content="  挂具查询  " Style="{StaticResource NavToggleBButtonStyle}"   
                  GroupName="BMenu"/>
            </StackPanel>
            <ContentControl prism:RegionManager.RegionName="AnalysisContentRegion" Margin="5"  />
        </DockPanel>

    </Grid>
</UserControl>
