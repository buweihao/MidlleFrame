<UserControl x:Class="BasicRegionNavigation.Views.Setting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Views"

             xmlns:Core="clr-namespace:Core"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="30*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="设备生产信息设置" FontSize="25" Background="Transparent"   Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,0"/>
        <ScrollViewer x:Name="SettingScriollviewer" HorizontalScrollBarVisibility="Auto" Grid.Row="1" Visibility="Visible">
            <Border  Margin="10" Background="#1E2A38" CornerRadius="6" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <!-- 表头 -->
                    <Grid Grid.Row="0" Background="#2e3a44">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="1.5*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="模组" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="1" Text="项目编号" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="2" Text="工序" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="3" Text="产品类别" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <!--<TextBlock Grid.Column="4" Text="产品颜色" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="5" Text="原料类别" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="6" Text="批次" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                        <TextBlock Grid.Column="7" Text="喷砂线体" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>-->
                        <TextBlock Grid.Column="8" Text="操作" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="5"/>
                    </Grid>

                    <!-- 数据行 -->
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding RowItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2,5,2,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="1.5*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- 模组名称 -->
                                    <TextBlock Text="{Binding ModuleName}"  Foreground="White" VerticalAlignment="Center" Margin="5"/>

                                    <!-- ComboBoxes -->
                                    <ComboBox Grid.Column="1" ItemsSource="{Binding ProjectCodes}" SelectedItem="{Binding SelectedProject}"  Margin="5,5,60,5"/>
                                    <ComboBox Grid.Column="2" ItemsSource="{Binding ProductTypes}" SelectedItem="{Binding SelectedProductType}" Margin="5,5,20,5"/>
                                    <ComboBox Grid.Column="3" ItemsSource="{Binding MaterialTypes}" SelectedItem="{Binding SelectedAnodeType}" Margin="5,5,20,5"/>
                                    <!--<ComboBox Grid.Column="4" ItemsSource="{Binding ProductColors}" SelectedItem="{Binding SelectedProductColor}" Margin="5,5,20,5"/>
                                    <ComboBox Grid.Column="5" ItemsSource="{Binding MaterialTypes}" SelectedItem="{Binding SelectedMaterialType}" Margin="5,5,20,5"/>
                                    <ComboBox Grid.Column="6" ItemsSource="{Binding Times}" SelectedItem="{Binding SelectedTimes}" Margin="5,5,20,5"/>
                                    <ComboBox Grid.Column="7" ItemsSource="{Binding BatchNumber}" SelectedItem="{Binding SelectBatchNumber}" Margin="5,5,20,5"/>-->

                                    <!-- TextBox -->
                                    <!--<TextBox Grid.Column="6" Background="#39444e" Foreground="Gray" BorderThickness="0" Text="{Binding BatchNumber}" Margin="5"/>-->

                                    <!-- Buttons -->
                                    <StackPanel Grid.Column="8" Orientation="Horizontal" HorizontalAlignment="Left">
                                        <Button Content="确认修改" Background="#409eff" Foreground="White" BorderThickness="0"  Command="{Binding ConfirmCommand}" Margin="10,0,20,0" Padding="10,2"/>
                                        <Button Content="内容重置" Foreground="Gray" Command="{Binding SettingCommand}" Margin="5,0" Padding="10,2"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </Grid>
            </Border>
        </ScrollViewer>

    </Grid>
</UserControl>
