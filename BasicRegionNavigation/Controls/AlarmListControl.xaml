<UserControl x:Class="BasicRegionNavigation.Controls.AlarmListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">
    <Border Background="#1E2A36"
            CornerRadius="6"
            Padding="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border CornerRadius="6,6,0,0"
                    Background="#2e3a44"
                    Height="35">
                <TextBlock Text="实时报警"
                           FontSize="16"
                           FontWeight="Bold"
                           Foreground="White"
                           VerticalAlignment="Center"
                           Margin="10,0,0,0" />
            </Border>

            <DataGrid ItemsSource="{Binding AlarmItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"                      Grid.Row="1"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      IsReadOnly="True"
                      RowHeight="36"
                      FontSize="14"
                      ColumnHeaderHeight="32"
                      Background="#1E2A36"
                      BorderThickness="0"
                      HorizontalGridLinesBrush="Transparent"
                      VerticalGridLinesBrush="Transparent">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="Background" Value="#1e242a" />
                        <Setter Property="BorderBrush" Value="#2e3a44" />
                        <Setter Property="Padding" Value="5,0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="HorizontalContentAlignment" Value="Left" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                        <Setter Property="Foreground" Value="Red" />
                        <Setter Property="Background" Value="#282b2e" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="4,0" />
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="序号" Binding="{Binding Index}" Width="60"/>
                    <DataGridTextColumn Header="时间" Binding="{Binding TimeFormatted}" Width="180"/>
                    <DataGridTextColumn Header="设备" Binding="{Binding Device}" Width="*"/>
                    <DataGridTextColumn Header="故障描述" Binding="{Binding Description}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>
