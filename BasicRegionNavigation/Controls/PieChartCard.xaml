<UserControl x:Class="BasicRegionNavigation.Controls.PieChartCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BasicRegionNavigation.Controls"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             x:Name="root">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Grid.Row="2" Background="#232f3a" CornerRadius="6" Padding="5" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="8*"/>
            </Grid.RowDefinitions>

            <ContentControl Content="{Binding Title, ElementName=root}" Style="{StaticResource SectionHeaderStyle}" />

            <lvc:PieChart Grid.Row="1"
                          Padding="0"
                          LegendPosition="Bottom"
                          LegendTextSize="14"
                          Background="Transparent"
                          Margin="0"
                          LegendTextPaint="{Binding White,ElementName=root}"
                          Series="{Binding Series, ElementName=root}"
                          Height="330"
                          Width="370"
                          IsClockwise="False"
                          InitialRotation="-90" />
        </Grid>
    </Border>
</UserControl>
